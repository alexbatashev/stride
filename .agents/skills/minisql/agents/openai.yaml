interface:
  display_name: "MiniSQL"
  short_description: "Use Stride's MiniSQL macro and typed query workflow"
  default_prompt: "Use $minisql to add or update schema, inserts, and selects with Stride's MiniSQL library."
